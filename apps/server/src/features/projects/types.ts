/**
 * Shared types for the projects feature.
 * Input/result types for handlers are co-located with their handlers.
 */

// Re-export schema types that are commonly used
export type { TaskStatus } from '../../db/schema';
