# Server URL for tRPC
VITE_SERVER_URL=http://localhost:3000

# Clerk authentication
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# Optional: Override redirect URLs after authentication (defaults to /select-project)
# VITE_AFTER_SIGN_IN_URL=/select-project
# VITE_AFTER_SIGN_UP_URL=/select-project
