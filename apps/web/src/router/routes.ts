/**
 * Centralized route definitions for client-side navigation validation.
 *
 * Re-exports from @agent-kit/shared to maintain a single source of truth.
 */

export { VALID_ROUTES } from '@agent-kit/shared';
export type { RouteDefinition } from '@agent-kit/shared';
