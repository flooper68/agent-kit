/**
 * Route validation utilities for client-side navigation.
 *
 * Re-exports from @agent-kit/shared to maintain a single source of truth.
 */

export { isValidRoute, validateRoute } from '@agent-kit/shared';
export type { ValidationResult } from '@agent-kit/shared';
