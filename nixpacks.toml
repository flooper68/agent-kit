# Disable providers to prevent auto-detection overriding our commands
providers = []

[phases.setup]
nixPkgs = ["nodejs_22", "bun"]

[phases.install]
cmds = ["bun i --no-save"]

[phases.build]
cmds = ["bun run build:ui && bun run build:web"]

[start]
cmd = "bunx serve apps/web/dist -s -l 3000"
